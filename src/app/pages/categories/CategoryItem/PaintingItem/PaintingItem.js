export default class PaintingItem {
  constructor(options, id) {
    this.author = options.author;
    this.name = options.name;
    this.year = options.year;
    this.path = options.painting;
    this.painting = options.painting;
    this.id = id;
  }
}
